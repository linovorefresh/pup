//import the regularSeasonStats array from the data file
import { players, regularSeasonStats } from "./data";

//return the array in descending order by points 
const descending = (statsArray) => {
    let reorderedStats = [];
    if(reorderedStats.length === 0) reorderedStats.push(statsArray[0]);

    i
}